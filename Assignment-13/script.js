const video = document.getElementById('background-video');

video.controls = false; // hide controls
// this button are used for the custom controls
video.addEventListener('play', () => {
    
});

video.addEventListener('pause', () => {
    
});
